[package]
name = "sample-service"
version = "0.2.0"
edition = "2021"

[dependencies]
tokio = { version = "1.32", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tracing = "0.1"
lib-common = { path = "../libs/lib-common" }

[dev-dependencies]
mockall = "0.11"
tokio-test = "0.4"

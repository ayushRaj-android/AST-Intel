"""Data models for AST Intel.

Pure dataclasses with no business logic. These define the canonical schema
for all AST nodes, workspace structures, cross-reference indexes, and
the code knowledge graph.
"""
